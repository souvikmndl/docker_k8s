apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: database-persistent-volume-claim
spec:
    accessModes:
        - ReadWriteOnce --> tells K8s to find a space on the node that has this permission
    resources:
        requests:
            storage: 2Gi --> we need 2GB of storage
